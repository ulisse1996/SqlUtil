package it.donatoleone.sqlutil.enums;

/**
 * Define Ordering Type used in {@link it.donatoleone.sqlutil.interfaces.From#orderBy(Ordering, String)} and
 * {@link it.donatoleone.sqlutil.interfaces.From#orderBy(Ordering, String...)}
 */
public enum Ordering {
    ASC,
    DESC
}
